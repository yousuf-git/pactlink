# Toggle the data layer. false = local seed data (offline, fully clickable),
# true = call the real backend at VITE_API_BASE.
VITE_USE_API=false
VITE_API_BASE=http://localhost:4000/api/v1
# Real publishable key when USE_API=true; ignored in mock mode.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
